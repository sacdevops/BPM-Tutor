# Web framework
flask>=2.3.0
flask-socketio>=5.3.0
python-socketio>=5.8.0
python-engineio>=4.5.0
eventlet>=0.33.0

# AI — HTTP client for CampusKI API
requests>=2.28.0

# Environment & config
python-dotenv>=1.0.0

# BPMN validation
lxml>=4.9.0

# Custom task file upload (PDF, DOCX)
pypdf>=3.0.0
python-docx>=1.0.0

# Benchmark
tiktoken>=0.5.0
openpyxl>=3.1.0
google-cloud-aiplatform[tokenization]>=0.1.0

# Headless bpmn.io executor (benchmark) — after install run: playwright install chromium
playwright>=1.40.0
